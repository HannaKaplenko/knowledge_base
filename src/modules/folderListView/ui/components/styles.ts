import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    backgroundColor: '#fff',
  },
  folderItem: {
    padding: 16,
    backgroundColor: '#2A3050',
    borderRadius: 10,
    marginBottom: 12,
  },
  folderText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
});
